# HuBMAP 肾小球分割项目说明与汇报提纲

## 1. 项目定位

这个项目的核心目标，是基于 HuBMAP 肾脏病理数据，构建一个能够自动分割肾小球区域的深度学习流程。它不仅仅是“把 U-Net 跑起来”，而是把整个流程整理成一个相对完整、可复现、可汇报、可继续迭代的工程：

- 原始输入是 Whole Slide Image, WSI 级别的 `.tiff`
- 标注来自多边形 `json`
- 训练前需要把大图切成可训练的小 patch
- 训练阶段需要稳定的指标体系和最佳权重保存机制
- 训练后需要独立评估、可视化分析和整张 TIFF 推理能力

从研究角度看，这是一个“病理图像语义分割”项目。
从临床辅助角度看，这是一个“自动定位和勾画肾小球区域”的工具型基础模块。

## 2. 为什么这个问题有意义

肾小球是肾脏病理分析中的关键结构。无论是做病理定量分析、下游特征提取，还是为医生提供更高效的阅片辅助，首先都需要尽量稳定地把肾小球区域找出来。

如果人工逐张病理切片去标注肾小球：

- 工作量很大
- 一致性受阅片者经验影响
- 面对大规模 WSI 时效率较低

因此，自动分割肾小球有几个直接价值：

- 为后续的病理定量研究提供标准化 ROI
- 减少人工初筛和粗标注负担
- 为下游实例检测、形态学分析、病理分级等任务打基础

## 3. 数据与任务特点

这个项目处理的数据不是普通自然图像，而是病理 WSI，具有几个非常典型的特点：

### 3.1 图像非常大

HuBMAP 原始数据是整张 TIFF，尺寸可达到数万像素级别。这样的图像不能直接送进常规 U-Net 训练，因此必须先切片。

### 3.2 标注不是现成 mask，而是 polygon JSON

每张 slide 一般对应三类文件：

- `slide_id.tiff`
- `slide_id.json`
- `slide_id-anatomical-structure.json`

其中：

- `slide_id.json` 中的 `properties.classification.name == "glomerulus"` 对应肾小球标注
- `slide_id-anatomical-structure.json` 中包含 `Cortex`、`Medulla` 等解剖结构标注

这意味着训练前不能直接读取一张 mask 图，而是要先把 polygon 栅格化成二值 mask。

### 3.3 类别极不平衡

在像素级分割中，背景像素远多于肾小球像素。也就是说：

- 大部分像素都属于背景
- 前景区域相对较小

这会直接带来两个后果：

- `Accuracy` 往往会偏高
- 评估时更应该重视 `Dice`、`IoU`、`Precision`、`Recall`

## 4. 本项目的整体技术路线

这个项目目前已经形成了一条比较完整的处理链：

1. 读取原始 HuBMAP TIFF
2. 读取肾小球 polygon JSON
3. 将 polygon 栅格化为二值 mask
4. 读取 anatomical JSON
5. 用 `Cortex` 作为 ROI 过滤不相关区域
6. 按固定窗口把 WSI 切成 patch
7. 过滤空白背景 patch
8. 控制正负样本比例
9. 以 patch 形式训练 U-Net
10. 在验证集上用多指标评估模型
11. 自动保存最优权重 `best.pth`
12. 用最佳权重做独立评估与整张 TIFF 推理

这条链路的价值在于，它把“数据处理、训练、评估、推理”串成了一个闭环，而不是停留在一次性实验脚本层面。

## 5. 为什么选择 U-Net

U-Net 是医学图像分割中的经典架构，尤其适合样本量有限但需要较精细空间定位的场景。

选择 U-Net 的原因主要有：

- 编码器负责提取语义特征
- 解码器逐步恢复空间分辨率
- skip connection 可以保留边界和局部细节
- 在医学分割任务中有大量成功经验

对于肾小球这样的结构分割任务，U-Net 不是最花哨的方案，但它是一个非常合理、稳健、容易工程化落地的基线。

## 6. 数据处理策略

本项目的数据处理不是简单切图，而是带有明确病理先验和工程约束的。

### 6.1 Polygon 栅格化

原始标注是多边形坐标，训练前必须转换为像素级 mask。这个过程决定了模型最终学到的监督信号是否正确，因此是整个流程中最关键的前处理之一。

### 6.2 Cortex ROI 过滤

肾小球主要分布在皮质区，因此使用 anatomical JSON 中的 `Cortex` 作为 ROI，有几个好处：

- 减少明显无关区域进入训练
- 降低背景噪声
- 提升正样本密度
- 让模型更聚焦于医学上合理的区域

这一步很重要，它不是单纯的“裁掉一些图”，而是在利用解剖先验提升训练样本质量。

### 6.3 组织区域过滤

病理 WSI 中会有很多空白背景。若这些 patch 大量进入训练：

- 会浪费计算资源
- 会进一步拉大正负样本不平衡
- 让模型更容易学会“预测背景”

因此脚本中加入了 `min-tissue-coverage` 这类过滤策略，用于剔除几乎全白的 patch。

### 6.4 正负样本控制

如果完全按滑窗切片，负样本数量会远超正样本。为避免模型过于偏向背景，本项目通过：

- `min-positive-pixels`
- `negative-ratio`
- `max-background-tiles-per-slide`

来控制训练样本分布。这本质上是在做样本层面的类别平衡。

### 6.5 按 slide 划分 train/val

验证集划分不是按 patch 随机混合，而是按 slide 层面切分。这样做更合理，因为同一张 WSI 的 patch 往往高度相似，如果把同一 slide 的 patch 同时放进 train 和 val，会导致验证结果偏乐观。

## 7. 训练策略

### 7.1 指标体系不再只看 Dice

最初项目只有 Dice，这对于快速试验足够，但不足以完整描述模型表现。现在训练和评估阶段统一输出：

- Dice
- IoU
- Precision
- Recall
- Specificity
- Accuracy
- validation loss

这样做的意义在于：

- Dice 和 IoU 反映整体重叠质量
- Precision 反映误检情况
- Recall 反映漏检情况
- Specificity 反映背景抑制能力
- Accuracy 仅作辅助参考

### 7.2 保存最佳权重而不是盲目存每个 epoch

之前一个比较明显的问题是：每个 epoch 都存一份 checkpoint，但没有明确告诉你哪一个最好。这样虽然“都存了”，但真正用起来并不方便。

现在训练逻辑会自动保存：

- `best.pth`
- `latest.pth`

其中：

- `best.pth` 根据设定指标自动更新
- `latest.pth` 表示最近一轮权重

这意味着训练结束后，不需要再人工猜测“到底该用哪个 epoch 的权重”。

### 7.3 可视化与分析结果自动落盘

训练过程中会同步生成：

- `history.csv`
- `training_curves.png`
- 最佳验证样本可视化
- `best_metrics.json`

这些文件的作用不只是看曲线，更重要的是方便后续汇报、复盘和与老师讨论。

### 7.4 训练提速策略

考虑到 HuBMAP 数据量较大、WSI 切片后样本数也不少，项目里加入了面向 4090 服务器的训练提速配置：

- `AMP` 混合精度
- `fused AdamW`
- `torch.compile`
- `TF32`
- `cuDNN benchmark`
- `persistent_workers`
- `prefetch_factor`
- `non_blocking` 数据传输

这些优化不改变任务本身，但能显著改善吞吐，让实验更可持续。

## 8. 当前项目已经完成的主要工作

到目前为止，这个项目已经不再是一个“原始 U-Net 模板”，而是一个面向 HuBMAP 肾小球分割的定制化工程。主要完成了以下工作：

### 8.1 数据准备能力

- 支持直接读取服务器现有目录中的 `tiff + json + anatomical json`
- 自动栅格化 glomerulus polygon
- 支持 `Cortex` ROI 过滤
- 支持异常 slide 的健壮处理
- 支持输出标准的 `train/images`、`train/masks`、`val/images`、`val/masks`

### 8.2 训练工程化

- 支持外部数据目录
- 支持更完整的指标体系
- 支持最佳权重保存
- 支持训练曲线和最佳样例可视化
- 支持吞吐优化参数

### 8.3 评估与推理

- 支持独立 checkpoint 评估
- 支持整张 TIFF 推理
- 支持 HSPN 场景推理脚本
- 推理脚本已经补齐 `--scale`
- checkpoint 加载 warning 已统一处理

### 8.4 文档整理

- README 已经覆盖服务器部署、数据准备、训练、评估、推理流程
- 当前这份文档进一步补充了项目思路、策略、结果解释和汇报话术

## 9. 当前 baseline 结果如何理解

基于当前这一版流程，项目得到的一轮代表性 baseline 结果是：

- 最优 epoch: 13
- 最优验证 Dice: 0.9292
- IoU: 0.8678
- Precision: 0.9291
- Recall: 0.9294
- Specificity: 0.9966
- Accuracy: 0.9935

这是一个比较好的结果，尤其是：

- Dice 已经超过 0.92
- Precision 和 Recall 很接近
- 说明模型不是单纯偏向“多报”或“少报”

对于分割任务来说，更值得重点汇报的是：

- Dice
- IoU
- Precision
- Recall

而不是单独强调 Accuracy。

## 10. 为什么 Accuracy 会很高

这个问题非常适合在汇报时提前解释。

在像素级分割中：

```text
Accuracy = (TP + TN) / (TP + TN + FP + FN)
```

由于背景像素远远多于肾小球像素，因此：

- `TN` 会特别大
- 只要背景判得不错，Accuracy 就会很高

所以在病理分割任务里，高 Accuracy 并不一定代表前景分割做得特别好。真正更有说服力的是 Dice 和 IoU，因为它们更关注前景区域的重叠质量。

这也是为什么本项目把 Accuracy 作为辅助指标，而不把它作为唯一主指标。

## 11. 为什么这次结果比过去的 0.77 高很多

这个问题不能简单理解成“模型突然变强了”，更合理的解释是：工程链路、数据质量和评价方式都比以前规范了。

最可能的原因包括：

### 11.1 数据处理更干净

以前的数据处理可能更偏一次性脚本或半手工流程，现在则更系统地做了：

- polygon 栅格化
- cortex ROI 限制
- 空白区域过滤
- 正负样本控制

这会显著提高训练样本质量。

### 11.2 最佳权重保存逻辑更合理

过去如果只是存了很多 `checkpoint_epochXX.pth`，但没有自动选最优模型，就很容易拿到一个并不是最佳的 epoch。

这次训练最终最优点在 `epoch 13`，而最后一轮已经回落到 `dice=0.8694`。如果只看最后一轮，很容易低估模型表现。

### 11.3 验证流程更清晰

现在训练时和训练后都可以用一致的评估逻辑独立验证，因此结果更可信，也更便于对比。

### 11.4 训练配置更稳

现在的训练配置、优化器选择、加载方式、吞吐优化，都比以前更适合这个任务。

## 12. 项目的当前优势

如果需要给老师或医生概括当前项目的优点，可以强调：

- 已经可以直接适配真实 HuBMAP 原始数据格式
- 已经形成完整的“数据处理到推理”的闭环
- 结果不只是能跑通，而是具备明确指标和最佳权重管理
- 工程化程度明显提升，便于复现和继续迭代
- 当前 baseline 结果已经达到较好的验证集表现

## 13. 当前局限性

为了汇报更客观，也建议主动说明当前局限：

- 当前仍以像素级分割指标为主，尚未系统引入实例级肾小球检出指标
- 当前结果主要基于 HuBMAP 数据内部验证，还需要更多外部数据验证泛化能力
- `predict_tiff.py` 目前还是简单的平铺式滑窗推理，后续可以继续优化边界融合策略
- 还没有系统评估不同 tile size、scale、ROI 策略对结果的影响
- 对 HSPN 等院内数据虽然已有脚本支持，但域偏移问题仍值得进一步研究

## 14. 下一步可以继续做什么

如果后续继续推进，这个项目有几个很自然的方向：

### 14.1 评价层面

- 加入实例级指标
- 统计单个肾小球的检出率、漏检率、误检率
- 做 slide 级可视化质控

### 14.2 模型层面

- 试验更强的编码器或注意力结构
- 比较不同输入尺度和 patch 策略
- 研究是否需要 boundary-aware loss 或 class-balanced loss

### 14.3 临床转化层面

- 在院内数据上做外部验证
- 观察不同染色风格、不同扫描设备下的表现
- 进一步思考如何与医生阅片流程结合

## 15. 给老师和医生汇报时可以怎么讲

下面是一版比较自然的汇报逻辑。

### 15.1 一句话版本

“我们基于 HuBMAP 肾脏病理数据，构建了一套从原始全视野病理图像、标注解析、自动切片、U-Net 训练、指标评估到整张图推理的肾小球自动分割流程，目前在验证集上取得了 Dice 0.9292 的结果。”

### 15.2 三句话版本

“这个项目解决的是肾小球自动分割问题。我们不是直接用现成 mask，而是从 HuBMAP 的原始 TIFF 和 polygon JSON 标注出发，先生成训练 patch，再训练 U-Net。现在已经形成了完整闭环，并且最佳验证 Dice 达到 0.9292，说明模型对肾小球区域具有较好的定位和分割能力。”

### 15.3 稍详细版本

“病理全视野图像非常大，不能直接输入网络，所以我们先把原始 TIFF 和对应的 glomerulus polygon 标注转换成 patch 级训练数据。同时利用 anatomical JSON 中的 Cortex 区域作为解剖先验，过滤掉不相关区域，以提高训练样本质量。训练阶段我们不再只看 Dice，而是同时统计 IoU、Precision、Recall 等指标，并自动保存最优权重。当前这一版 baseline 在验证集上 Dice 达到 0.9292，说明整体方案已经具备较好的可用性，也为后续做更细的病理量化分析打下了基础。”

## 16. 汇报时可能会被问到的问题

### 16.1 为什么不直接处理整张 WSI

因为 WSI 太大，显存无法直接承受，所以需要切片。切片也是当前病理深度学习中很常见的处理方式。

### 16.2 为什么 Accuracy 这么高

因为背景像素远多于肾小球像素，Accuracy 容易被大量背景真负样本拉高，所以更应该看 Dice 和 IoU。

### 16.3 为什么这次结果比以前高很多

主要是因为这次数据处理、样本过滤、验证逻辑和最佳权重管理都更加规范，而不只是网络结构本身改变。

### 16.4 这个结果是否已经可以直接用于临床

当前更适合定位为研究型和辅助型结果，说明方案可行、性能较好，但距离正式临床应用仍需要更充分的外部验证与稳定性评估。

## 17. 一句话总结

这个项目目前已经从“能跑通的 U-Net 实验”提升成了“面向 HuBMAP 肾小球分割的完整工程流程”，既能够支持研究复现，也能够支持向老师和医生进行较清晰的阶段性汇报。
